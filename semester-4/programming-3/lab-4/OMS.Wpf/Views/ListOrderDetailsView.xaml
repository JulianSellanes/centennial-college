<UserControl x:Class="OMS.Wpf.Views.ListOrderDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="Select Basket:"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       Margin="0,0,10,0"/>

            <ComboBox Width="320"
                      ItemsSource="{Binding Baskets}"
                      SelectedItem="{Binding SelectedBasket}"
                      DisplayMemberPath="DisplayText"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding BasketItems}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IdBasketItem" Binding="{Binding IdBasketItem}" Width="110"/>
                <DataGridTextColumn Header="IdProduct" Binding="{Binding IdProduct}" Width="90"/>
                <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="90"/>
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="110"/>
                <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat=C}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Add Item to Order"
                    Width="140"
                    Height="32"
                    Command="{Binding AddItemToOrderCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>