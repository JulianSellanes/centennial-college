<Window x:Class="OMS.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OMS.Wpf"
        xmlns:viewModels="clr-namespace:OMS.Wpf.ViewModels"
        xmlns:views="clr-namespace:OMS.Wpf.Views"
        mc:Ignorable="d"
        Title="Order Management System"
        Height="520"
        Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:ListOrderDetailsViewModel}">
            <views:ListOrderDetailsView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:AddNewItemViewModel}">
            <views:AddNewItemView />
        </DataTemplate>
    </Window.Resources>

    <Grid Background="#F5F5F5" Margin="10">
        <Border Background="White" Padding="15" CornerRadius="6" BorderBrush="#D0D0D0" BorderThickness="1">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
    </Grid>
</Window>
