<Window x:Class="PMSManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PMSManager.UserControls"
        xmlns:viewModel="clr-namespace:PMSManager.ViewModels"
        mc:Ignorable="d"
        Title="Final Exam" 
        Height="450" 
        Width="800"
        FontSize="14">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:ListEditAllTransactionsViewModel}">
            <local:ListEditAllTransactionsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:AddNewTransactionViewModel}">
            <local:AddNewTransactionView />
        </DataTemplate>
    </Window.Resources>

    <Window.DataContext>
        <viewModel:MainWindowViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Background="CadetBlue">
            <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center">
                <TextBlock Text="Sales Transaction Manager"
                       FontSize="30"
                       VerticalAlignment="Center"
                       FontFamily="Ink Free" />
            </StackPanel>
        </Border>
        <StackPanel Grid.Row="1"
                Orientation="Horizontal">
            <Button Content="List Sales Transaction"
                Width="150"
                Background="CornflowerBlue"
                Foreground="Yellow"
                Command="{Binding View1Command }" />
            <Button Content="Add New Sales Transaction"
                Width="221"
                Background="LightBlue"
                Foreground="Yellow"
                Command="{Binding View2Command }"></Button>
            <Button Content="Exit"
                Width="150"
                Background="CornflowerBlue"
                Foreground="Yellow"
                Command="{Binding ExitCommand}" />
        </StackPanel>
        <ContentControl Grid.Row="2"
                    Content="{Binding CurrentViewModel}"></ContentControl>
    </Grid>
</Window>
